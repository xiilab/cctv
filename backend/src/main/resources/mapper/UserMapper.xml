<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiilab.cctv.user.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.xiilab.cctv.user.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="email" property="email"/>
        <result column="full_name" property="fullName"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="selectAllUsers" resultMap="UserResultMap">
        SELECT * FROM users ORDER BY id
    </select>

    <select id="selectUserById" parameterType="long" resultMap="UserResultMap">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <select id="selectUserByUsername" parameterType="string" resultMap="UserResultMap">
        SELECT * FROM users WHERE username = #{username}
    </select>

    <select id="selectUsersByStatus" parameterType="string" resultMap="UserResultMap">
        SELECT * FROM users WHERE status = #{status}
    </select>

    <select id="selectUsersByKeyword" parameterType="string" resultMap="UserResultMap">
        SELECT * FROM users 
        WHERE full_name LIKE CONCAT('%', #{keyword}, '%') 
           OR email LIKE CONCAT('%', #{keyword}, '%')
    </select>

    <insert id="insertUser" parameterType="com.xiilab.cctv.user.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, email, full_name, status, created_at, updated_at)
        VALUES (#{username}, #{email}, #{fullName}, #{status}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="updateUser" parameterType="com.xiilab.cctv.user.User">
        UPDATE users 
        SET username = #{username}, 
            email = #{email}, 
            full_name = #{fullName}, 
            status = #{status}, 
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteUser" parameterType="long">
        DELETE FROM users WHERE id = #{id}
    </delete>

</mapper>
